{
  "title": "DynamoDB Stream to EventBridge using Splitter pattern",
  "description": "This pattern takes a change data capture event from DynamoDB and splits it into multiple events into EventBridge."
}